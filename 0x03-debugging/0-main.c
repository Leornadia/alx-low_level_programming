#include "main.h"

/**
 * main - tests the positive_or_negative function with a case of 0
 * Return: Always 0
 */
int main(void)
{
    int i;

    i = 0;
    positive_or_negative(i);

    return (0);
}

